<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juanjomorcillo.actvideojuegosjuanjomorcillo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Actvideojuegosjuanjomorcillo">
        <activity android:name=".enlaceweb"></activity>
        <activity android:name=".incrementar" />
        <activity android:name=".precionintendo" />
        <activity android:name=".precioplay5" />
        <activity android:name=".precios" />
        <activity android:name=".Microsoft" />
        <activity android:name=".Nintendo" />
        <activity android:name=".Sony" />
        <activity android:name=".Fabricantes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>